// Optional GPT recommendation backend.
// Leave empty on GitHub Pages to use the local rule-based recommender.
// If the site is deployed with the included Vercel function, use "/api/recommend".
window.AI_RECOMMEND_API = "";
